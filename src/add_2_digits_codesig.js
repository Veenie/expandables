//example, if n =29, you want to add 2+9 to get 11

// function solution(n) {
    
//     str = n.toString()
    
    //changes to string
    
    // return +str[0] + +str[1]

//+ in front is unary operator, allows us to add the values
// }
function stringIncludes(wrd, ltr){
   	 let match;
   	 for(let i = 0; i < wrd.length; i++){
     	 if(wrd[i] === ltr){
        match = true
      }
    }
    return !!match
 	 }

//less costly version

